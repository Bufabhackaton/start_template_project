#!/usr/bin/env bash
# PostToolUse Cline hook - macOS/Linux shim.
# Hands stdin/stdout/stderr to the cross-platform Node implementation in lib/.
# See ./README.md for the contract and what the hook does.
set -e
exec node "$(dirname "$0")/lib/post-tool-use.mjs"
